# Copy to .env (gitignored). Local dev uses SQLite; the Postgres migration plan
# (migrate-the-registry-database-from-sqlite-to-postgres) swaps this for a
# postgresql:// URL. The SQLite path is resolved relative to prisma/.
DATABASE_URL="file:./dev.db"
