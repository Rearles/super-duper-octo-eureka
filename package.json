{
  "name": "mound-city",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Mound City — a cold-case detective game (MVP prototype). See docs/GameConceptDocument.md.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  }
}
